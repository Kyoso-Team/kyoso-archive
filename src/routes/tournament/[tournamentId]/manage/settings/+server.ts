// import { redirect } from '@sveltejs/kit';
// import type { RequestHandler } from './$types';

// export const GET = (async (event) => {
//   throw redirect(302, `${event.url.href}/general`);
// }) satisfies RequestHandler;
