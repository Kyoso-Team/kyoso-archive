import '../utils/mocks';
import { trpc } from '$lib/trpc';
import { describe, expect, it, vi } from 'vitest';
import { createCaller } from '../utils/trpc';

describe('tRPC prodecures: Tournaments', () => {
  it('test', () => {
    expect(1).toBe(1);
  });
});